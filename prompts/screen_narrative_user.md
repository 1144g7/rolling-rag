## 已记录的叙事（你的输出会直接拼接到这后面，不要重复其中已有的任何内容）
{prev_narrative}

## 历史摘要（按时间顺序）
{summaries}

## 近期键盘操作
{keyboard_events}

## 输入状态
{input_state}

## Token 信息
当前已用 {token_used} / {token_max} tokens。请根据剩余空间决定 keep_summary_count 和 keep_keyboard_count。

---

请根据以上上下文和当前截图，判断当前帧的叙事类型，输出JSON。
