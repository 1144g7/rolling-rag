# Rolling RAG · 提示词注册表
# 两个概念：片段(fragment) + 组合(recipe)
# 片段 = 一个命名文本单元（文件或动态生成）
# 组合 = 片段名的列表，按顺序拼接

fragments:
  # ── 文件片段 ──
  - name: conv_unified
    file: conv_unified.md

recipes:
  # 对话索引层（统一滚动，短+长）
  - name: conv_unified
    fragments:
      - conv_unified
